kind: Secret
apiVersion: v1
metadata:
  name: {{ .Values.name }}
stringData:
  {{- range $key, $val := .Values.secrets.stringData }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
data:
  {{- range $key, $val := .Values.secrets.data }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end }}